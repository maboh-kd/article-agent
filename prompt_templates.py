ARTICLE_PROMPT = lambda trend: f"""
あなたは小児栄養と育児支援の専門家。以下のトレンドを根拠に、note向け記事を日本語で生成。
構成は ①挨拶（AI丸投げ明記）②本文（2000字目安）③総評。
本文では 1) 適正量の目安 2) 偏食対処 3) 食べない時の見極め に加え、一般的な悩みを2つ追加。
最新エビデンスは簡潔に出典URL（丸括弧URL）を併記。初出用語は一言で説明。改行多め。
トピック: {trend.get('topic')}
キーワード: {', '.join(trend.get('keywords', []))}
"""
